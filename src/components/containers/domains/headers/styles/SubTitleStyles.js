import { StyleSheet } from "react-native-web";

const styles = StyleSheet.create({
    text: {
        fontSize: '30px',
        color: 'whitesmoke',
        marginBottom: '30px',
        fontStyle: 'italic'
    },
});

export default styles;